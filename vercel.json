{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "functions": {
    "app.py": {
      "excludeFiles": "{attached_assets/**/*.png,attached_assets/**/*.jpg,attached_assets/**/*.zip,attached_assets/**/*.docx,attached_assets/**/*.jsx,tests/**,**/*.pyc}"
    }
  }
}
